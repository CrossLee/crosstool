#!/bin/bash

set -euo pipefail
export PATH=/usr/bin:/bin:/usr/sbin:/sbin
umask 077
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd -P)"
source "$SCRIPT_DIR/migration-common.sh"
onepaw_initialize "${3:-/}"
onepaw_postinstall "$SCRIPT_DIR/expected-app.plist"
